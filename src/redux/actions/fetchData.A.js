import database from '../../startup/db_init';

const getPodcastData = () => {
  return async dispatch => {
    try {
      // firebase.auth().signInWithEmailAndPassword(email, password);
    } catch (e) {
      // Fetch fail
      console.error(e);
    }
  };
};
